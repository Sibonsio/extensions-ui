import '../components/Card.css'
import '../components/formCard.css'
const newCard = ({ handleClick, id, lightBg, darkText, textColor, toggleClick, isActive }) => {
    return (
        <></>
    )
}

export default newCard